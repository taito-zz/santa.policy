Log in as the portal owner::

    >>> from plone.app.testing import TEST_USER_NAME, TEST_USER_PASSWORD
    >>> browser.login(TEST_USER_NAME, TEST_USER_PASSWORD)

Continue your test::

    >>> browser.open('/partners/++add++santa.Partner')

    >>> browser.getControl(name="form.widgets.IDublinCore.title").value = 'Partner01'
    >>> browser.getControl(name="form.widgets.IDublinCore.description").value = 'Description of Partner01.'
    >>> browser.getControl(name="form.widgets.address").value = 'Streat 12345'
    >>> browser.getControl(name="form.widgets.post_code").value = '678910'
    >>> browser.getControl(name="form.widgets.email").value = 'partner01@partner.com'
    >>> browser.getControl(name="form.buttons.save").click()

    >>> print browser.bycss('.documentFirstHeading')
    --- 1 ---
    <h1 class="documentFirstHeading">Partner01</h1>

    >>> print browser.bycss('.documentDescription')
    --- 1 ---
    <p class="documentDescription">Description of Partner01.</p>

    >>> browser.open('/partners/partner01/en/edit')

    >>> browser.getControl(name="title").value = 'PARTNER001'
    >>> browser.getControl(name="description").value = 'DESCRIPTION OF PARTNER001.'
    >>> browser.getControl(name="text").value = '<p>BODY OF PARTNER001.</p>'
    >>> browser.getControl(name="form.button.save").click()

    >>> browser.open('/partners/partner01')

    >>> print browser.bycss('#santa-partner')
    --- 1 ---
    <section id="santa-partner"><h1 class="documentFirstHeading">PARTNER001</h1>
    <p class="documentDescription">DESCRIPTION OF PARTNER001.</p>
    <p>BODY OF PARTNER001.</p>
    </section>
